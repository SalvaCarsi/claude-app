services:
  db:
    image: postgres:16-alpine
    container_name: claude-app-db
    environment:
      POSTGRES_USER: claude
      POSTGRES_PASSWORD: claude123
      POSTGRES_DB: claude_app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
